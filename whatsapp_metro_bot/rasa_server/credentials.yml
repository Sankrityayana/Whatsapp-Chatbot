# This file contains credentials for the components around Rasa.
# You don't need to add anything here for this example project.

rest:
#  # This endpoint is used for hosting the Rasa server itself.
#  # Endpoint where you can retrieve the running model.
#  # rasa run --enable-api

# rasa_x:
#   # The URL of the Rasa X server. This is only needed if you want to
#   # connect your bot to Rasa X.
#   url: "http://localhost:5002/api"
